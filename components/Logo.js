import { taprom } from "@/lib/fonts";

export default function Logo() {
  return (
    <div>
      <h1 className={`${taprom.className} text-6xl font-bold text-center`}>
        តោះ<span className="text-amber-500">ឆាត</span>
      </h1>
    </div>
  );
}
